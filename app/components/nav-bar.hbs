<nav class="flex items-center justify-between flex-wrap bg-teal-500 p-6">
    <div class="block lg:hidden">
        <button
            class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                <title>Menu</title>
                <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
            </svg>
        </button>
    </div>
    <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
        <div class="text-sm lg:flex-grow">
            <LinkTo @route="index" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 text-xl hover:text-white mr-4">
                Home
            </LinkTo>
            <LinkTo @route="table" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 text-xl hover:text-white mr-4">
                Table
            </LinkTo>
            <LinkTo @route="image-page" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 text-xl hover:text-white">
                Image uploader
            </LinkTo>
        </div>
        {{#if session.isAuthenticated}}
        <div>
            <a href="#"
                class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 text-xl hover:bg-white mt-4 lg:mt-0"
                {{on "click" this.logout}}>Logout</a>
        </div>
        {{/if}}
    </div>
</nav>

{{!-- <nav class="bg-white shadow">
    <div class="container flex items-center justify-center px-6 py-8 mx-auto text-gray-600 capitalize">
        <LinkTo @route="index" class="text-gray-800 border-b-2 border-teal-500 mx-1.5 sm:mx-6">home</LinkTo>

        <LinkTo @route="table"
            class="border-b-2 border-transparent hover:text-gray-800 hover:border-teal-500 mx-1.5 sm:mx-6">table
        </LinkTo>

        <LinkTo @route="image-page"
            class="border-b-2 border-transparent hover:text-gray-800 hover:border-teal-500 mx-1.5 sm:mx-6">image upload
        </LinkTo>

        {{#if session.isAuthenticated}}
        <div>
            <a
                class="h-10 leading-10 border-b-2 border-dotted md:border-teal-600 md:border-solid md:border-2 md:rounded-full md:w-24 md:leading-9" {{on "click" this.logout}}>Logout</a>
        </div>
        {{/if}}
    </div>
</nav> --}}